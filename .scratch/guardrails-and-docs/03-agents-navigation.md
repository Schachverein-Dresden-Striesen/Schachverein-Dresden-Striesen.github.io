# 03: Update AGENTS.md with navigation pointer to setup docs

**What to build:** Add a context pointer to AGENTS.md directing agents to the README setup section when they need to configure the development environment. This reduces navigation friction by making setup discoverable and naming the condition for reaching it.

**Blocked by:** 02: Expand README.md with setup and usage documentation

**Status:** ready-for-agent

- [ ] Add a "Setup & Development Environment" section to AGENTS.md
- [ ] Include a navigation pointer that names the README sections and lists the triggering conditions (e.g., "when setting up the environment", "when running tests fails with import errors")
- [ ] Briefly document the `src/` working directory requirement for imports
- [ ] Keep the pointer concise (3–4 lines max); avoid restating information that is already in README
